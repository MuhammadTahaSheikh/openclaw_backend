# OpenClaw Backend

Express + TypeScript API for OpenClaw.

## Setup

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start dev server with hot reload on port 3000
- `npm run build` — compile TypeScript
- `npm start` — run compiled server

## Endpoints

- `GET /` — welcome message
- `GET /health` — health check
